// All data is now derived from siteConfig — edit siteConfig.ts to customise.
import config from './siteConfig';

export const SERVICES = config.services;
export const PROCESS_STEPS = config.processSteps;
export const TESTIMONIALS = config.testimonials.reviews;
export const LOCATIONS = config.contact.locations;
export const TRUST_BADGES = config.trustBadges;
export const STATS = config.testimonials.stats;
export const PARTNERS = config.partners;
export const LIFESTYLE_PILLARS = config.lifestylePillars;
